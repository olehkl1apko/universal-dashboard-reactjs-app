import { menu } from "./menu";
import {
  IAreaData,
  areaData,
  revenueData,
  IBarData,
  visitsData,
} from "./charts";

export { menu, areaData, revenueData, visitsData };
export type { IAreaData, IBarData };
